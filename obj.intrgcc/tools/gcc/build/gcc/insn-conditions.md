(define_conditions [
  (-1 "(TARGET_HARD_FLOAT) && (!TARGET_64BIT)")
  (-1 "!TARGET_64BIT && TARGET_HARD_FLOAT
   && (register_operand (operands[0], DFmode)
       || reg_or_0_operand (operands[1], DFmode))")
  (-1 "(flag_pic) && (Pmode == DImode)")
  (-1 "TARGET_64BIT && (REGNO (operands[0]) == REGNO (operands[3]) || peep2_reg_dead_p (2, operands[0]))
   && (GET_CODE (operands[4]) != ASHIFT || (CONST_INT_P (operands[2]) && INTVAL (operands[2]) < 32))")
  (-1 "(flag_pic && SYMBOL_REF_LOCAL_P (operands[1])) && (TARGET_64BIT)")
  (-1 "(riscv_split_symbol (operands[2], operands[1], MAX_MACHINE_MODE, NULL)) && (Pmode == DImode)")
  (-1 "(!flag_pic || flag_pie) && (Pmode == SImode)")
  (-1 "(flag_pic) && (Pmode == SImode)")
  (-1 "(TARGET_MULDIV) && (TARGET_64BIT)")
  (-1 "Pmode == DImode")
  (-1 "(register_operand (operands[0], QImode)
    || reg_or_0_operand (operands[1], QImode))")
  (-1 "(!TARGET_64BIT && (flag_pic && SYMBOL_REF_LOCAL_P (operands[0]))) && (TARGET_64BIT)")
  (-1 " reload_completed && REG_P (operands[1])")
  (-1 "SIBLING_CALL_P (insn)")
  (-1 "(riscv_split_symbol (operands[2], operands[1], MAX_MACHINE_MODE, NULL)) && (Pmode == SImode)")
  (-1 "TARGET_HARD_FLOAT")
  (-1 "(TARGET_HARD_FLOAT && !HONOR_SIGNED_ZEROS (DFmode)) && (TARGET_HARD_FLOAT)")
  (-1 "!TARGET_64BIT && (flag_pic && SYMBOL_REF_LOCAL_P (operands[0]))")
  (-1 "TARGET_MULDIV")
  (-1 "TARGET_ATOMIC")
  (-1 "(register_operand (operands[0], HImode)
    || reg_or_0_operand (operands[1], HImode))")
  (-1 "(TARGET_HARD_FLOAT && !TARGET_64BIT && (flag_pic && SYMBOL_REF_LOCAL_P (operands[0]))) && (TARGET_HARD_FLOAT)")
  (-1 "! TARGET_64BIT")
  (-1 "flag_pic && SYMBOL_REF_LOCAL_P (operands[1])")
  (-1 "(TARGET_64BIT) && ( reload_completed && register_operand (operands[1], VOIDmode))")
  (-1 "(reload_completed) && (TARGET_64BIT)")
  (-1 "TARGET_64BIT && (GET_CODE (operands[2]) == CONST_INT ? INTVAL (operands[2]) < 32 : 1)")
  (-1 "(register_operand (operands[0], SImode)
    || reg_or_0_operand (operands[1], SImode))")
  (-1 "TARGET_SOFT_FLOAT
   && (register_operand (operands[0], SFmode)
       || reg_or_0_operand (operands[1], SFmode))")
  (-1 "TARGET_SOFT_FLOAT
   && (register_operand (operands[0], DFmode)
       || reg_or_0_operand (operands[1], DFmode))")
  (-1 "(TARGET_HARD_FLOAT) && (TARGET_64BIT)")
  (-1 "(TARGET_ATOMIC) && (TARGET_64BIT)")
  (-1 "reload_completed")
  (-1 "!TARGET_64BIT
   && (register_operand (operands[0], DImode)
       || reg_or_0_operand (operands[1], DImode))")
  (-1 "!TARGET_MEMCPY")
  (-1 "TARGET_64BIT
   && (register_operand (operands[0], TImode)
       || reg_or_0_operand (operands[1], TImode))")
  (-1 "TARGET_64BIT
   && (register_operand (operands[0], DImode)
       || reg_or_0_operand (operands[1], DImode))")
  (-1 "(TARGET_HARD_FLOAT && TARGET_FDIV) && (TARGET_HARD_FLOAT)")
  (-1 "reload_completed && !TARGET_64BIT
   && riscv_split_64bit_move_p (operands[0], operands[1])")
  (-1 "(TARGET_64BIT && (GET_CODE (operands[2]) == CONST_INT ? INTVAL (operands[2]) < 32 : 1)) && (TARGET_64BIT)")
  (-1 "Pmode == SImode")
  (-1 "riscv_can_use_return_insn ()")
  (-1 "TARGET_64BIT && TARGET_HARD_FLOAT
   && (register_operand (operands[0], DFmode)
       || reg_or_0_operand (operands[1], DFmode))")
  (-1 "TARGET_MULDIV && TARGET_64BIT")
  (-1 "TARGET_64BIT && (flag_pic && SYMBOL_REF_LOCAL_P (operands[0]))")
  (-1 "(TARGET_HARD_FLOAT && TARGET_64BIT && (flag_pic && SYMBOL_REF_LOCAL_P (operands[0]))) && (TARGET_HARD_FLOAT)")
  (-1 "TARGET_HARD_FLOAT
   && (register_operand (operands[0], SFmode)
       || reg_or_0_operand (operands[1], SFmode))")
  (-1 "TARGET_64BIT")
  (-1 "TARGET_MULDIV && !TARGET_64BIT")
  (-1 "(TARGET_64BIT && (flag_pic && SYMBOL_REF_LOCAL_P (operands[0]))) && (TARGET_64BIT)")
  (-1 "TARGET_64BIT && INTVAL (operands[2]) < 32")
  (-1 "TARGET_64BIT && (REGNO (operands[0]) == REGNO (operands[3]) || peep2_reg_dead_p (2, operands[0]))")
  (-1 "(!flag_pic || flag_pie) && (Pmode == DImode)")
  (-1 "(TARGET_HARD_FLOAT && TARGET_64BIT && flag_pic && SYMBOL_REF_LOCAL_P (operands[1])) && (TARGET_HARD_FLOAT)")
  (-1 "TARGET_HARD_FLOAT && TARGET_64BIT")
  (-1 "(TARGET_HARD_FLOAT && !TARGET_64BIT && flag_pic && SYMBOL_REF_LOCAL_P (operands[1])) && (TARGET_HARD_FLOAT)")
  (-1 "(TARGET_HARD_FLOAT && !HONOR_SIGNED_ZEROS (SFmode)) && (TARGET_HARD_FLOAT)")
  (-1 "TARGET_64BIT && reload_completed")
  (-1 "TARGET_64BIT && IN_RANGE (INTVAL (operands[2]), 32, 63)")
  (-1 "(TARGET_64BIT) && ( reload_completed && REG_P (operands[1]))")
])
